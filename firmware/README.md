# 固件目录

待创建 STM32CubeMX/IDE 工程后，将自动生成的底层文件与手写应用层分开保存：

- `Core/`：启动代码、`main`、RTOS 初始化和任务创建。
- `Drivers/`：对 ADC、PWM、OLED、按键、UART 等外设的轻量封装。
- `App/`：温度换算、滤波、PID、状态机、保护和通信协议。
- `Config/`：引脚、温度阈值、PID 默认参数等配置。

不要在此仓库提交从公司获得的固件、配置文件或产品专有协议。
